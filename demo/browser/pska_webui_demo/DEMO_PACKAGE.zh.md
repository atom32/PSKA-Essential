# Legacy Package Disabled

这个旧 demo package 已禁用。

不要再从 `pska_webui_demo` 生成对外演示包；PSKA 没有独立产品前端。对外演示必须使用 Hermes WebUI 内的 `pska-mini` extension、Hermes chat turn、PSKA HTTP MCP、sidecar proxy、review/task 投影和 Eidolia bridge。

新的演示包入口：

```text
demo/browser/hermes_pska_extension_demo/
```
